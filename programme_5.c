// Hii there !! this is a programme to calculate the area of a rectangle
// Area of a rectangle = length*breadth
main()
{
    int length , breadth ;
    printf("The respective length and breadth of the rectangle are = ");
    scanf("%d/%d", &length , &breadth );
    getch();
    printf("The area of the given rectangle = %d", length*breadth );
    getch();
}
