// Hii there !! this is a simnple addition programme with input given by user
main()
{
    int a , b ;
    printf("The values of a and b respectively are = ");
    scanf("%d/%d", &a,&b);
    // please give / between the values of a and b in output
    getch();
    printf("The sum of a and b = %d ", a+b );
    getch();
}

