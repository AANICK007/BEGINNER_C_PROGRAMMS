// Hii there !! this is a simple program to calculate area of the circle
// Area of circle = 3.143*radius*radius
main()
{
    int radius , area ;
    printf("The radius of the circle = ");
    scanf("%d" , &radius );
    getch();
    area= (3.14)*(radius*radius) ;
    printf("The area of the circle is = %d", area );
    getch();
}
